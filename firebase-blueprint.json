{
  "entities": {
    "User": {
      "title": "User",
      "description": "A user in the system.",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "name": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "joinedAt": { "type": "number", "description": "Timestamp in milliseconds" },
        "ordersCount": { "type": "number" }
      },
      "required": ["email", "name", "role", "joinedAt", "ordersCount"]
    },
    "Cake": {
      "title": "Cake",
      "description": "A cake product in the catalog.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "price": { "type": "string" },
        "desc": { "type": "string" },
        "tag": { "type": "string" },
        "gauge": { "type": "string" },
        "gaugeVal": { "type": "string" },
        "img": { "type": "string" },
        "createdAt": { "type": "number", "description": "Timestamp in milliseconds" }
      },
      "required": ["title", "price", "desc", "img", "createdAt"]
    },
    "Order": {
      "title": "Order",
      "description": "A cake order.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "customer": { "type": "string" },
        "amount": { "type": "string" },
        "paidAmount": { "type": "string" },
        "paymentMethod": { "type": "string" },
        "status": { "type": "string", "enum": ["Pending", "Pending Verification", "Confirmed (Pending Balance)", "Fully Paid", "Preparing", "Delivered", "Cancelled"] },
        "createdAt": { "type": "number", "description": "Timestamp in milliseconds" }
      },
      "required": ["userId", "customer", "amount", "paidAmount", "paymentMethod", "status", "createdAt"]
    },
    "Inquiry": {
      "title": "Inquiry",
      "description": "An event inquiry.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "email": { "type": "string" },
        "eventDate": { "type": "string" },
        "occasionType": { "type": "string" },
        "vision": { "type": "string" },
        "status": { "type": "string", "enum": ["Pending", "Needs Reply", "Resolved"] },
        "createdAt": { "type": "number", "description": "Timestamp in milliseconds" }
      },
      "required": ["name", "email", "eventDate", "occasionType", "vision", "status", "createdAt"]
    },
    "BlogPost": {
      "title": "Blog Post",
      "description": "An article in the food blog.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "excerpt": { "type": "string" },
        "content": { "type": "string" },
        "category": { "type": "string" },
        "img": { "type": "string" },
        "author": { "type": "string" },
        "date": { "type": "string" },
        "tips": { "type": "array", "items": { "type": "string" } },
        "likes": { "type": "number" },
        "createdAt": { "type": "number", "description": "Timestamp in milliseconds" },
        "updatedAt": { "type": "number", "description": "Timestamp in milliseconds" }
      },
      "required": ["title", "content", "img"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "User profiles. Managed by the users themselves and admins."
    },
    "/cakes/{cakeId}": {
      "schema": { "$ref": "#/entities/Cake" },
      "description": "Cake products. Modifiable only by admins."
    },
    "/orders/{orderId}": {
      "schema": { "$ref": "#/entities/Order" },
      "description": "Cake orders."
    },
    "/inquiries/{inquiryId}": {
      "schema": { "$ref": "#/entities/Inquiry" },
      "description": "Event inquiries."
    },
    "/blog_posts/{postId}": {
      "schema": { "$ref": "#/entities/BlogPost" },
      "description": "Blog articles. Publicly readable, modifiable by admins, likes can be incremented by anyone."
    }
  }
}
